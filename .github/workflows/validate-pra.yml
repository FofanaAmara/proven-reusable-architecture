name: Validate PRA

# Workflow for validating PRA technical quality
# Runs on: pull_request (opened, synchronize)
#
# Validates:
# - MDX format and YAML frontmatter
# - Metadata completeness (name, category, status, tags, proven_in_use, etc.)
# - Required sections (Overview, Context, Architecture, ADR, Examples, Proven-in-use)
# - Internal/external links (no 404s)
# - Scope/path coherence (secteurs/* vs transversal/*)
# - Status/proven-in-use count coherence

on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - 'site/content/**/registre/**/*.md'
      - 'site/content/**/registre/**/*.mdx'

jobs:
  validate:
    name: Validate PRA Files
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: |
          npm install gray-matter

      - name: Get changed PRA files
        id: changed-files
        uses: tj-actions/changed-files@v41
        with:
          files: |
            site/content/**/registre/**/*.md
            site/content/**/registre/**/*.mdx
          files_ignore: |
            site/content/**/registre/index.md
            site/content/**/registre/guides/**

      - name: Validate PRA files
        if: steps.changed-files.outputs.any_changed == 'true'
        run: |
          echo "Changed PRA files:"
          echo "${{ steps.changed-files.outputs.all_changed_files }}"

          # Initialize error counter
          ERROR_COUNT=0

          # Validate each changed file
          for file in ${{ steps.changed-files.outputs.all_changed_files }}; do
            echo ""
            echo "========================================="
            echo "Validating: $file"
            echo "========================================="

            # Skip if file doesn't exist (deleted)
            if [ ! -f "$file" ]; then
              echo "⚠️  File deleted, skipping validation"
              continue
            fi

            # Validate metadata
            echo ""
            echo "📋 Checking metadata..."
            if node scripts/validate-pra-metadata.js "$file"; then
              echo "✅ Metadata validation passed"
            else
              echo "❌ Metadata validation failed"
              ERROR_COUNT=$((ERROR_COUNT + 1))
            fi

            # Validate structure
            echo ""
            echo "📐 Checking structure..."
            if node scripts/validate-pra-structure.js "$file"; then
              echo "✅ Structure validation passed"
            else
              echo "❌ Structure validation failed"
              ERROR_COUNT=$((ERROR_COUNT + 1))
            fi
          done

          # Summary
          echo ""
          echo "========================================="
          echo "Validation Summary"
          echo "========================================="
          echo "Files checked: ${{ steps.changed-files.outputs.all_changed_files_count }}"
          echo "Errors found: $ERROR_COUNT"

          # Fail if any errors
          if [ $ERROR_COUNT -gt 0 ]; then
            echo ""
            echo "❌ Validation failed with $ERROR_COUNT error(s)"
            exit 1
          else
            echo ""
            echo "✅ All validations passed!"
            exit 0
          fi

      - name: No PRA files changed
        if: steps.changed-files.outputs.any_changed == 'false'
        run: |
          echo "ℹ️  No PRA files were changed in this PR"
          echo "Validation skipped"
